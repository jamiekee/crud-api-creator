package apiCreator.logging;

import org.slf4j.Logger;
import org.slf4j.LoggerFactory;

public class APICreatorLogging {
    public static final Logger logger = LoggerFactory.getLogger(APICreatorLogging.class);
}
